name: Publish latest kubectl version if newer

on:
  schedule:
    - cron: '0 0 * * 2'  # Every Tuesday at 00:00 UTC
  workflow_dispatch:  # Allow manual trigger

jobs:
  check-version:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' || github.event_name == 'workflow_call'
    outputs:
      version: ${{ steps.get-version.outputs.version }}
      should_publish: ${{ steps.check-tag.outputs.should_publish }}
    steps:
      - name: Get latest kubectl version
        id: get-version
        run: |
          LATEST_VERSION=$(curl -s https://dl.k8s.io/release/stable.txt)
          echo "Latest kubectl version: $LATEST_VERSION"
          
          # Remove 'v' prefix and extract major.minor (e.g., v1.35.2 -> 1.35.2)
          VERSION=$(echo "$LATEST_VERSION" | sed 's/^v//' | cut -d'.' -f1,2,3)
          echo "::notice::Version (major.minor.patch): $VERSION"
                    
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Check if tag exists on Docker Hub
        id: check-tag
        run: |
          VERSION="${{ steps.get-version.outputs.version }}"
          TAG_URL="https://hub.docker.com/v2/repositories/fabionaspolini/kubectl/tags/${VERSION}"
          
          echo "Checking if tag '$VERSION' exists on Docker Hub..."
          
          # Check if tag exists (HTTP 200 = exists, HTTP 404 = not found)
          HTTP_STATUS=$(curl -s -o /dev/null -w "%{http_code}" "$TAG_URL")
          
          if [ "$HTTP_STATUS" = "200" ]; then
            echo "::notice::Tag '$VERSION' already exists on Docker Hub."
            echo "should_publish=false" >> $GITHUB_OUTPUT
          elif [ "$HTTP_STATUS" = "404" ]; then
            echo "::warning::Tag '$VERSION' does not exist. Will publish."
            echo "should_publish=true" >> $GITHUB_OUTPUT
          else
            echo "::error::Error on check if image tag already exists."
            echo "should_publish=false" >> $GITHUB_OUTPUT
            exit 1
          fi

  publish:
    needs: check-version
    if: needs.check-version.outputs.should_publish == 'true'
    uses: ./.github/workflows/build-push.yml
    secrets: inherit
    with:
      version: ${{ needs.check-version.outputs.version }}
      push_latest: 'true'
